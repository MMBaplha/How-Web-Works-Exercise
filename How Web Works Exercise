- What is HTTP?
# Hypertext Transfer Protortype are how browser and servers communicate.

- What is a URL?
# Uniform Resource Locator are the wed page address that has an protocol and hostname and file name. http://site.com/some/page.html?x=1

- What is DNS?
# Domain Name System is an system that takes the URL and converts into IP addresses.

- What is a query string?
# Query are extra information and info from forms with an key-value pair in the URL.

- What are two HTTP verbs and how are they different?
# GET: get some data from server
# POST: sends some data to the server

- What is an HTTP request?
# Is a request from a client to a server which follows the HTTP protocol.

- What is an HTTP response?
# Is a response from a server to a client which follows the HTTP protocol 

- What is an HTTP header? Give a couple examples of request and response headers you have seen.
# The HTTP header is extra information about an request or response.
# The Header request consist of: Cache-Control, Cookie, Host
# The Header repsonse consist of: Content-Type, Date, Access-Control-Allow-Methods 

- What are the processes that happen when you type “http://somesite.com/some/page.html” into a browser?
#1. The browser take the domain name into an IP address using DNS
#2. The browser makes a request to that IP address. 
#3. The server sends a response code. 
#4. The browser makes a DOM from that HTML, and finds any other resources needed (images, CSS, JavaScript, etc)
#5. The browser makes separate HTTP requests for those resources and receives response from the server for each